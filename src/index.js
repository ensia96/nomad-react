import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root")); // 컴포넌트 : HTML 을 반환하는 함수 | <App /> 과 같은 모양으로 사용 | JS 와 HTML 사이의 조합을 JSX 라고 부름
